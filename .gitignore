# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Build binaries
slackbot-winston
main

# Vendor dir generated by deps manager
vendor/

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out
